# The port your Express server should run on
PORT=4000

# An example secret key for JWT or API tokens (optional for now)
SECRET_KEY=your-secret-here

# Database connection string / URL
DATABASE_URL=mongodb://localhost:27017/hackathon-db

# Example public/private API keys (if calling an external service or AI model)
OPENAI_API_KEY=your-openai-key

# (Optional) AI service URLs or Flask microservice endpoints
AI_SERVER_URL=http://localhost:5000/predict
